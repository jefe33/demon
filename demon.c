#include "synchro.h"

int main(){
    // openlog(NULL, LOG_PID | LOG_CONS, LOG_DAEMON);
    // sync_folders("/opt/zadania/test/src", "/opt/zadania/test/dest", true, 1024 * 1024 * 10); // 10MB
    // closelog();
}